# Changelog for logback.xml Configuration Updates (April 30, 2026)

## Summary

This document summarizes the recent updates made to the `logback.xml` configuration files across multiple repositories, improving structured logging and observability.

## Updates

- Added structured JSON logging using `LogstashEncoder` to enhance log readability and integration with log management systems.
- Introduced asynchronous appenders (`ASYNC_CONSOLE`, `ASYNC_FILE`) to improve logging performance by offloading logging tasks from main application threads.
- Configured `RollingFileAppender` with time-based rolling policy to manage log file size and retention (logs rotated daily with a max history of 30 days).
- Set root logging level to `INFO` with references to asynchronous console and file appenders to ensure consistent logging behavior.
- Added specific logger levels for frameworks such as Spring and Hibernate to reduce log verbosity by setting their levels to `ERROR`.
- Standardized `logback.xml` configurations across all repositories to ensure uniform observability and logging practices.

## Repositories Affected

- payments-core
- transaction-engine
- api-gateway
- auth-service
- webhook-service

## Impact

These changes improve the observability and performance of logging in the respective services, facilitating better monitoring and troubleshooting capabilities.

---

*This changelog was autogenerated based on recent commits to logback.xml configurations.*
